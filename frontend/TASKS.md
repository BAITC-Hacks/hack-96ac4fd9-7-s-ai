# TASKS — FRONT

Порядок жёсткий: сначала T1–T5 (MVP end-to-end), потом T6+. T7 необязателен.

- [ ] T1 Каркас: Vite+React+strict TS+Tailwind, один экран, layout, порт 5173,
      proxy /api → http://localhost:8787, npm run typecheck
- [ ] T2 `src/api/client.ts` (getHealth, getCatalogOptions, createMatch) + `src/api/mocks.ts`
      с 3 фикстурами found/no_category/no_match, `VITE_USE_MOCKS=true`;
      варианты found: плотная (3), редкая (1–2), занятая дата (3 → 1).
      Детерминированность, причины отсева в message; fetch только в client.ts.
- [ ] T3 Форма: город, дата, тип мероприятия, категория, бюджет (обязательные) +
      длительность, язык (опциональные, по умолчанию скрыты) — опции из `getCatalogOptions()`.
      Валидация, загрузка, защита от повторного submit, минимальный end-to-end вывод ответа.
- [ ] T4 Экран результата: до 3 карточек ИЛИ явное сообщение по `status`
      (`found`/`no_category`/`no_match`), краткое резюме параметров запроса сверху
      с возможностью поменять дату/бюджет и повторить; message и candidatesBeforeCut.
      Снимок выполненного запроса; занятость только из ответа API, без догадок по ID.
- [ ] T5 Карточка: название/категория/город/цена/объяснение + пометка `dataFlags`,
      если не пустой; 1–2 предложения на основе фактов, без рейтингов/звёзд/процентов.
      Ручное демо: плотная/редкая категории, смена даты 3 → 1, оба пустых исхода.
- [ ] T6 После MVP: адаптивность, доступность, загрузка, ошибки каталога/сети/API,
      повтор без потери ввода, production build, инструкция демо
- [ ] T7 (если время останется) сравнение 2-3 карточек рядом / доп. фичи из бэклога
- [ ] T8 По запросу пользователя: интерфейс Қазақша / Русский / English, переключатель,
      сохранение выбора; переводы полей, состояний, ошибок, дат, цен и пометок.
      Локализовать моки, сохранять канонические значения каталога и язык подрядчика.
      Контракт реального backend не менять; описать запрос на язык ответов.

## Проверки и журнал

После каждого шага: npm run typecheck; lint, если есть; ручная проверка;
отчёт: что сделано, что дальше, можно коммитить: да/нет.

- План сверён с семью файлами; T1–T5 включены в MVP, порядок сохранён.
  До T1 typecheck недоступен: package.json ещё нет. Приложение пока не готово к коммиту.
- T1: код готов, npm install и typecheck прошли, Vite отвечает на 5173.
  Ручной UI-check заблокирован: Computer Use не смог достоверно определить URL браузера
  и прекратил управление на этот turn. Поэтому checkbox до ручной проверки не закрыт.
- T2: код готов, typecheck и scripts/checkMocks.mjs прошли; вручную прочитаны ответы
  3 → 1 → 0 и причины занятости. UI-check ожидается вместе с формой.
  Контрактные уточнения записаны в CONTRACT_REQUEST.md. Коммитить: нет, UI ещё не проверен.
- T3: форма подключена к клиенту, обязательные поля и опции каталога, скрытые дополнительные
  параметры, валидация и блокировка submit. typecheck пройден; HTTP главной страницы и App.tsx: 200.
  Ручная проверка UI остаётся заблокированной; минимальный путь запроса реализован.
- T4: сводка запроса с быстрым редактированием даты/бюджета; 3 отдельных состояния,
  message и счётчики, очистка старой выдачи, повтор запроса. typecheck и SSR-проверки
  scripts/checkUi.mjs прошли (включая клиент → mocks). Ручной браузерный прогон ожидается.
- T5: все поля карточек, цена «от», фактические объяснения и три dataFlags реализованы.
  typecheck, SSR-проверки и production build прошли. MVP код закончен;
  checkbox оставлен открытым до ручного end-to-end прогона.
- T6: обработаны ошибки загрузки каталога, API, сети, неверный JSON/форма ответа;
  доступны повторы без потери введённых параметров. Добавлены адаптивная сетка,
  подписи полей, focus-visible, live status, disabled во время запроса и README с демо.
  npm run typecheck, npm run check (3 набора), npm run build — PASS.
  Главная страница, преобразованный клиент и CSS от dev-сервера — HTTP 200.
  Lint не настроен. T7 не начинался.

## Итог текущей сессии

- [x] Код T1–T5 (MVP) реализован строго по порядку, после него реализован T6.
- [x] Автоматические проверки и production build проходят.
- [x] Три фикстуры, редкий результат и смена даты 3 → 1 → 0 проверены.
- [x] CONTRACT_REQUEST.md и README.md актуальны.
- [ ] Ручной браузерный прогон: заблокирован инструментом Computer Use (не определён URL).
- [ ] Интеграция с настоящим backend: пока работаем с VITE_USE_MOCKS=true.

Верхние T1–T6 не отмечены полностью готовыми до требуемой ручной проверки.
Коммитить: нет по критерию AGENTS.md, нужен ручной прогон по README.
Git-команды не выполнялись; изменения только в frontend/; .env не менялся.
