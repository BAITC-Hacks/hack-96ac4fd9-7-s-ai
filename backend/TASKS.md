# TASKS — BACK

Порядок жёсткий: сначала T1-T5 (MVP end-to-end), потом T6+.

- [x] T1 Каркас: Express+TS, CORS, роуты-заглушки `/api/health`, `/api/catalog-options`,
      `/api/match` с правильной формой `ApiResponse<T>`
- [x] T2 Загрузка CSV-датасета в память (`backend/data/`), парсинг `busy_dates` в
      сравнимый формат, нормализация категорий/городов/языков
- [x] T3 `GET /api/catalog-options` — реальные списки из датасета (уникальные city/
      categories/event_formats/languages)
- [x] T4 `POST /api/match` — жёсткие фильтры (город/категория/формат/занятость/
      бюджет-как-порог/язык/длительность) + детерминированный отбор топ-3 + определение
      `status` (`found`/`no_category`/`no_match`) + `candidatesBeforeCut` — без AI, на
      шаблонных объяснениях, чтобы путь end-to-end работал первым
- [x] T5 `src/ai/index.ts`: один batch-вызов OpenAI (фолбэк NVIDIA) на весь запрос,
      короткий таймаут, замена шаблонных объяснений на AI-объяснения при успехе
- [x] T6 Кеш удачных AI-ответов (`backend/data/cache.json`) для устойчивости демо
- [ ] T7 (если время останется) `/api/match` доп. поле с причиной по каждому отсеянному
      кандидату («что если поменять дату/бюджет») — под будущую фичу фронта

## Қабылдау шарттары және жұмыс журналы

- T1–T7 реті сақталады. T7 ортақ келісім өзгерісінсіз іске асырылмайды.
- T2: бастапқы CSV форматы, 66 бірегей ID, тілдің ru/kz/en mapping-і және max_hours=null тексеріледі.
- T4: баға тек шек; рейтингте баға жоқ; бірдей ұпайда ID шешеді.
- T4: no_category қала+санат бойынша тексеріледі; барлық шығару себептері message ішінде айтылады.
- T4: қысқа ерекше сипаттама фактісі template түсіндірмесінде де болады.
- T5: batch, OpenAI → NVIDIA → template, қысқа жалпы deadline; кілттер мен провайдер қате денелері логқа шықпайды.
- Әр қадамнан кейін typecheck және curl smoke орындалады; lint конфигурациясы болса іске қосылады.
- Міндетті тексеріс: тығыз/сирек санат, екі бос күй, бір сұраныс екі рет, екі күн, тіл/ұзақтық/бюджет шекаралары.
- Git әрекеттері тек адамның нақты рұқсатымен орындалады. Кейін адам осы backend өзгерістерін commit/push жасап, main-ға біріктіруге рұқсат берді.

### Бастапқы күй

GitHub main нұсқасы оқылды, тек backend өзгертіледі. CSV репозиторийде жоқ;
бұрын берілген Google Drive CSV көзі backend/data/contractors.csv жолына алынады.

T1: `npm run typecheck` өтті. curl арқылы health, catalog-options және match
заглушкалары HTTP 200 / ApiResponse пішімінде тексерілді. Коммит жасауға болады: иә.
csv-parse қосымша пакетін орнатуға адам нақты рұқсат берді.

T2: CSV 66 бірегей профильге оқылды. Тізімдер `|`, күндер `YYYY-MM-DD`,
бос max_hours → null, қазақ тілі → kz. 3 тест пен typecheck өтті, health curl өтті.
Коммит жасауға болады: иә. Деректегі ID және белгілер сақталды.

T3: typecheck өтті; curl нақты 3 қала, 17 санат, 6 формат және en/kz/ru тілдерін қайтарды.
Коммит жасауға болады: иә. FRONT осы мәндерді өзгеріссіз жіберуі тиіс.

T4: typecheck, 10 тест және scripts/smoke.ps1 ішіндегі curl тексерістері өтті.
6 кандидат → 3 карточка; күн ауысса 2 карточка; сирек санат 1; екі бос status дұрыс.
Қайталанған сұраныстың ID реті бірдей. Рейтингке баға кірмейді. Коммит: иә.

T5: typecheck, 16 тест және curl smoke өтті. Әр провайдерге 1800 мс, барлығы 3600 мс.
AI барлық карточкаға бір batch-та дерек үзіндісі мен дәл факт сөйлемін таңдайды;
сервер оларды 1–2 сөйлемге біріктіреді. Жаңа факт ойдан қосылмайды.
SDK сұраныстары stub transport-пен тексерілді; нақты API кілттері бұл ортада жоқ.
Кодты коммит жасауға болады: иә. Нақты провайдермен live smoke әлі қажет.

T6: тек жарамды AI таңдаулары сақталады. Кэш сұраныс, датасет, prompt/model,
рейтинг және нақты дәлел нұсқаларымен байланысты. Қайта іске қосу, ескі нұсқа
және бүлінген кэш тесттері өтті. Typecheck, 17 тест және curl smoke өтті.
Коммит: иә. Нақты API кілттері жоқ болғандықтан production кэші әлі жылытылған жоқ.

T7: кейінге қалдырылды; shared келісіміне жаңа өріс қосуға рұқсат жоқ.

## Қалған сыртқы тексерістер

- [ ] Адам ортаға API кілтін қауіпсіз қосқаннан кейін нақты OpenAI/NVIDIA шақыруын тексеру.
- [ ] FRONT-тың нақты қосымшасымен бірлескен браузер smoke test.
- BACK → main жариялауды адам тапсырды; орындалған commit/push нәтижесі GitHub тарихынан тексеріледі.

## Соңғы тексеріс

Backend сыртындағы 16 файлдың SHA-256 мәндері бастапқы көшірмемен бірдей.
FRONT, shared, түбірлік ережелер өзгермеді. Typecheck және 17 тест өтті.
Curl smoke барлық міндетті сценарийден өтті; соңғы template жауап HTTP 200,
шамамен 0.0044 с (жергілікті өлшем, live AI жылдамдығы емес).
Lint script берілмеген. Өзгерген файлдар тізімі backend/README.md ішінде.
Түбірлік ignore-ды өзгертпей, backend/.gitignore ішінде бастапқы CSV-ге exception жасалды.
